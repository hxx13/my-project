---
description: 微信小程序 aroapp 列表/表单页按钮与选择控件统一风格（检查维护等模块）
---

# 小程序按钮与选择器

- **避免**整行独占的默认大按钮（`van-button` + `block` 铺满宽）作为列表/卡片常规操作；主操作与次要操作使用 **圆角胶囊**（`border-radius: 999rpx`），同一行可并排多个。
- **避免**原生 `<picker>`、原生 `<button>` 作为 UI 主交互外观；用 **自定义 `view` 条（fm-select-chip）+ `van-popup` 选项列表** 完成选择，样式与主题色一致。
- **卡片操作**：操作按钮放在 **标题同一行右侧**（`flex` + 标题 `ellipsis`），副标题/说明占第二行；避免按钮另起一行撑满导致溢出。
- **主题**：机构中国红主色 `#ac1736`，与首页快捷入口一致；主按钮可用 `linear-gradient(135deg, #c41e3a, #ac1736)`。
