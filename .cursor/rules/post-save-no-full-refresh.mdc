---
description: 保存/更新成功后禁止整表或整页重载；用接口返回或本地合并单条（含小程序）
alwaysApply: true
---

# 保存后禁止整页/整表刷新

## 约束（前端 Web、管理后台、微信小程序 `aroapp/` 等）

在用户对**单条记录**执行「保存」「提交」「更新」等写操作且成功后：

- **不要**调用会**替换整页列表**的重新加载（例如整表 `load()` / `loadData({ reset: true })` / `location.reload()` / 仅为了同步一条数据而 `refetch` 整张表），除非用户**显式**触发了刷新（如筛选变更、下拉刷新、进入页首次加载、用户点击「刷新」按钮）。
- **应当**使用：写接口返回体 + **就地合并**（`setState` / `setData` 更新对应 id 的那一行）、或 React Query 的**单实体** `setQueryData`，保持滚动位置与未改动行的 UI 状态。

## 注释要求

实现保存逻辑时，在成功回调附近用简短中文注释标明「保存后仅合并当前行，禁止整表 load」，并指向本规则文件名 `post-save-no-full-refresh.mdc`，便于检索与 Code Review。

## 例外

- 批量导入、全量同步、用户确认的「从 CSV 刷新」等**本就需要全量重拉**的操作，可以整表刷新。
- 若后端未返回更新后字段且无法推导，可退而求其次**只请求该单条** GET，仍避免重拉整列表。
