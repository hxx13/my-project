name: Mindmap Sync Check

on:
  pull_request:
    paths:
      - 'src/main/java/com/example/demo/modules/**'
      - 'frontend/src/**'
      - 'scripts/mindmap/**'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check mindmap sync
        run: npm run mindmap:check

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: [
                '## ⚠ Mindmap 同步检查失败',
                '',
                '`mindmap.yaml` 与当前源码不一致。这可能是因为：',
                '- 新增或删除了 Java 模块/Controller/API',
                '- 新增或删除了前端页面/路由',
                '',
                '### 修复方法',
                '在本地运行以下命令更新后重新提交：',
                '',
                '```bash',
                'npm run mindmap',
                'git add scripts/mindmap/mindmap.yaml docs/mindmap/',
                'git commit -m "chore: sync mindmap"',
                '```',
              ].join('\n')
            });
