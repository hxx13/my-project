# 08 — 修复优先级与策略

> P0-P4 五阶段路线图。合并自原审计第七节（旧版 P0-P4）与八-F 节（OWASP 对齐后的更新版），综合四轮审计全部 57 项发现。

---

## P0 — 立即执行（6 小时内，10 项极危 + 阻断级数据泄露）

> 目标：消除所有零知识远程接管路径和已确认的数据泄露向量。完成后攻击者无法在无任何前置知识的条件下接管系统。

| 顺序 | 编号 | 操作 | 耗时 |
|------|------|------|:---:|
| 1 | NS1 | 删除 `JwtTokenService.java` 第 77-87 行 LEGACY_MOCK_PREFIX 分支 + 第 119 行绕过 + 前端 `authStorage.ts` MOCK_JWT_PREFIX + `getUserIdFromToken()` | 10min |
| 2 | NS7 | `OpenApiConfig.java` 加 `@Profile("!prod")`，或 `application.properties` 设 `springdoc.api-docs.enabled=false` | 2min |
| 3 | NS2 | `SpecialChannelService.login()` 删除 `Boolean faceVerified` 参数，改为接受 `faceVerifyToken`（String）并调用 `validateFaceVerifyToken()` | 1h |
| 4 | NS6 | PIN 端点（`pin-status` / `set-pin`）添加身份验证 — 要求携带 JWT 或 faceVerifyToken，`set-pin` 成功后不再自动签发 JWT 除非验证通过 | 30min |
| 5 | NS27 | `WebMvcConfig.java:36` 将 `addPathPatterns("/api/admin/**")` 改为 `addPathPatterns("/api/admin/**", "/api/admin-nav/**")` | 15min |
| 6 | NS28 | `TwinScanController` 7 端点 + `TwinApiController` dashboard user-status 端点添加 `requireOperator()` 或等效认证；短期至少补 `ApiAuthInterceptor` 路径覆盖 | 2h |
| 7 | NS5 | 删除 `AuthController.java` 密码重置中的自动创建 `sys_user` 逻辑；`/forgot-password/verify` 签发一次性 HMAC resetToken（5 分钟 TTL）；`/forgot-password/reset` 强制验证该 token | 1h |
| 8 | NS13 | `AdminMapper.xml` 两个查询（`getPersonnelWithAuth` / `getSystemOnlyUsers`）中移除 `su.password` 列；替换为 `CASE WHEN su.password IS NOT NULL THEN 1 ELSE 0 END as passwordSet` | 30min |
| 9 | NS12 + NS25 | 服务端 `NotificationService.java:208` 使用 `HtmlUtils.htmlEscape(content)` 转义；前端 `student-notifications.tsx:274` 加 `DOMPurify.sanitize()`；全量审计其余 7 处 `dangerouslySetInnerHTML` | 1h |
| 10 | NS29 | 全部 `/api/v1/twin/dashboard/debug/**` 9 个端点添加 `@Profile("!prod")` 或 SUPER_ADMIN 角色检查 | 1h |

---

## P1 — 第 1-3 天（安全准入基线，18 项）

> 目标：完成全部 28 项生产准入条件。消除所有阻断级缺陷，建立可验证的安全基线。

| 顺序 | 编号 | 操作 | 耗时 |
|------|------|------|:---:|
| 11 | NS14 + NS22 | `sys_user` 表加 `token_version` 列；JWT claims 包含 version；`AdminAuthInterceptor` 验证时重新从数据库加载角色而非信任 claims；密码修改/角色变更/强制下线时递增 version | 4h |
| 12 | S1 | JWT 密钥轮换（`openssl rand -base64 32`）→ 环境变量 `JWT_SECRET` 注入 → 删除 `application.properties` 默认值 → `git filter-branch` 清除历史 | 2h |
| 13 | NS4 | AES 密钥通过环境变量 `APP_PASSWORD_AES_KEY` 注入 → 删除 `AesEncryptionService.java` 硬编码后备值 → 改为启动时抛异常 | 1h |
| 14 | NS3 | 云函数代理密钥 `app.mp.proxy.secret` 通过环境变量注入强随机值（`openssl rand -hex 32`） | 30min |
| 15 | S3 | `/forgot-password/reset` 统一错误消息（不区分 "userId 不存在" 和 "手机号不匹配"）；配合 NS5 的 resetToken 验证 | 2h |
| 16 | S4 | `AuthImpersonationController.java:44` 加 `if (user.getRole().getLevel() < RoleEnum.SUPER_ADMIN.getLevel()) return Result.fail(403, ...)` | 15min |
| 17 | S5 | `FaceBaselineController.getBaselines()` 加 `requireAdmin()`；`proxyImage()` 加 URL 白名单（仅允许已知 CDN/存储域名）；移除 `file://` 协议支持 | 2h |
| 18 | S6 + S10 | nginx 启用 SSL 证书（已有 `*.shsmu.edu.cn` 泛域名）→ HTTP→HTTPS 301 跳转 → 添加 HSTS / X-Frame-Options / X-Content-Type-Options / Referrer-Policy / CSP 头 | 1h |
| 19 | NS19 | `StudentMobileCenterController` 移动令牌端点改为从当前认证用户读取 userId（而非接受路径参数）；或添加 STAFF+ 权限检查 | 30min |
| 20 | NS21 | `application.properties` 添加 `server.servlet.session.cookie.http-only=true` / `secure=true` / `same-site=strict` | 15min |
| 21 | NS18 | 关键状态变更端点（密码修改、角色变更、删除操作）添加自定义 `Origin` / `X-Requested-With: XMLHttpRequest` 头校验 | 4h |
| 22 | NS23 | 遗留调试端点（`/api/v1/twin/rpg/**`、`/api/v1/twin/prediction/admin/trigger`、`/api/v1/monitor/**`）添加 `@Profile("!prod")` 或 SUPER_ADMIN 检查 | 1h |
| 23 | NS24 | `AuthController` 登录端点添加账号级别失败计数（3 次失败锁定 15 分钟或要求验证码），与 `AuthEndpointRateLimitFilter` IP 限流形成纵深 | 2h |
| 24 | S2 | `ALTER TABLE sys_user DROP COLUMN password_plain` → 移除 `UserMapper.xml` 中 `updatePasswordWithPlainById` / `getPasswordPlainById` → 修改 7 处调用方（AdminService、StaffRegistrationService、AuthController、StudentRegistrationService） | 1 天 |
| 25 | S7 | 创建 `application-prod.properties` → 全部敏感值改为 `${ENV_VAR}` 占位符 → 删除默认值 → 创建 `.env.example` 模板 | 1 天 |
| 26 | S9 | `SocketIOConfig.java` 设 `setOrigin("https://aroultra.shsmu.edu.cn")` → mobile channel 加连接限流 | 1h |
| 27 | S12 | nginx `proxy_set_header X-Forwarded-For $remote_addr`（替换 `$proxy_add_x_forwarded_for`），或在应用层取最右/最后 IP | 30min |
| 28 | NS16 | 全部 `Map<String, Object>` @RequestBody 改为强类型 DTO 类 + `@NotNull` / `@NotBlank` / `@Valid` 校验（20+ Controller 方法，优先高危：AdminNavConfig、Upload、Asset） | 1 天 |

---

## P2 — 第 1 周（技术债清理，约 18 项）

> 目标：清理可被自动化/半自动化修复的技术债务，消除已知的中高危非阻断缺陷。

| 编号 | 操作 | 耗时 |
|------|------|:---:|
| NS8 | 删除 `ReportFormController.java:34` 和 `ReportFillController.java:31` 的 `@CrossOrigin("*")` | 5min |
| NS9 | `UploadController.java:148-152` 路径穿越防护对齐 — 添加 `normalize()` + `startsWith()` 双重校验 | 30min |
| NS10 | `LlmConfigService.java:51-57` LLM Base URL 添加主机白名单（仅允许已知 AI 服务商域名） | 1h |
| NS11 | 删除 `CommonAsyncService.java:30-32` 遗留 `Runtime.getRuntime().exec()` 死代码 | 5min |
| S17 | SQL 拼接改为 `?` 参数化绑定 | 15min |
| S18 | 文件上传主通道添加扩展名白名单 + 路径穿越防护归一化 | 30min |
| S20 | 补充多处缺失的限流端点（Filter 配置） | 1h |
| S21 | `pom.xml` fastjson2 2.0.40 → 最新稳定版 | 5min |
| NS15 | 全部 Mapper XML 从 `SELECT *` 改为显式列名列表（渐进式，高危表优先：sys_user、aro_personnel、sys_student_notification） | 4h |
| NS20 | PII 日志脱敏 — 生产环境将 userId+IP 日志降级为 DEBUG，或对 userId 做哈希化处理后记录 | 1h |
| NS30 | 扫码助手跨用户 AI 对话 IDOR — 添加权限检查 | 30min |
| NS31 | `KnowledgePageRenderer.tsx:13` 富文本路径 `contentHtml` 加 `sanitizeRichTextHtml()` 包裹 | 5min |
| NS32 | `SuppliesService.java` / `MaterialService.java` 库存扣减加 `SELECT ... FOR UPDATE` | 2h |
| NS33 | 全量 DTO 补充 Jakarta Validation 注解（`@Min`/`@Positive`/`@NotNull`/`@NotBlank`/`@Size`） + Controller 加 `@Valid` | 2 天 |
| NS34 | MySQL `useSSL=true` + WinCC/Dahua TLS 验证启用（`ssl-insecure=false`） | 1h |
| S13 | 196 `@Autowired` 字段注入 → 构造函数注入（44 文件机械性重构，可脚本辅助） | 1 人周 |

---

## P3 — 空闲期（架构演进，约 12 项）

> 目标：在常规需求迭代的间隙逐步推进，不设硬性截止日期但持续进行。

| 编号 | 操作 | 耗时 |
|------|------|:---:|
| S14 | Controller 去 Mapper — 33 个 Controller 的 Mapper 调用提取到 Service（渐进式，每次 1 个 Controller-Mapper 对） | 1 人周（分散） |
| S15 | God Service 拆分 — 36 个超 500 行 Service 按子域拆分（每次 1 个，先拆 MaterialService 1,930 行和 AssetService 1,885 行） | 2-4 人周（分散） |
| S19 | 配置管理 290 行单片文件拆分为多个 `application-{module}.properties` | 半天 |
| S22 | Spring Boot 版本评估 — 3.5.11-SNAPSHOT 是否等待正式发布或降级到 3.5.10 | 调研半天 |
| NS25 扩展 | 全量 `dangerouslySetInnerHTML` 使用点安全审计 + 自动化 ESLint 规则禁止裸用 | 半天 |
| — | CI/CD 管道安全 — 构建流水线添加 SAST 扫描 + 依赖漏洞检查 + SBOM 生成（OWASP A08） | 2 天 |
| — | 全量端点的 BOLA/IDOR 系统性审计 — 基于 OpenAPI spec 逐端点验证对象级授权 | 1 周 |
| — | 生产环境异常统一屏蔽 — 确保所有异常不泄露堆栈/内部路径 | 2h |
| — | XSS CSP 头细化 — 从 `default-src 'self'` 开始逐步收紧 | 1 天 |
| NS35 | GDPR/PIPL 合规 — 自助账户删除 + 数据导出/便携性（Art.20） + 同意追踪（Art.7） + 身份证号 API 脱敏 | 1 周 |

---

## P4 — 远期（架构重设计 / 合规，约 5 项）

> 目标：需要专门立项或跨团队协调的架构级变更。不在日常迭代范围内。

| 编号 | 操作 | 说明 |
|------|------|------|
| S16 | twin 模块 15 路辐射依赖解耦 | 引入事件总线或应用服务层，将中心辐射型依赖转为发布-订阅。影响 15+ 模块，需专门立项 |
| — | OAuth 2.0 / OpenID Connect 认证体系迁移 | 替换当前自研 JWT 体系为标准协议，获得刷新令牌、令牌吊销、联合登录等能力 |
| — | BFF（Backend For Frontend）模式引入 | 解决 JWT 存 localStorage 的安全问题：Token 完全服务端管理，前端仅持有 httpOnly Cookie 的 Session |
| — | 零信任内网架构 | 扫码终端等端点从"公网路径 + 代码认证"迁移到独立端口 + 硬件防火墙内网隔离 |
| — | 等级保护 2.0 正式测评 | 基于 NS17 审计日志 + 全量安全加固后，启动等保测评流程 |

---

## 时间线总览

| 阶段 | 时间 | 项数 | 累计准入 | 说明 |
|------|------|:---:|:---:|------|
| P0 | 6 小时 | 10 | 10/28 ✅ | 极危零知识接管全部消除，系统从"任何人可接管"降为"需已认证身份" |
| P1 | 4-6 工作日 | 18 | 28/28 ✅ | 全部准入条件通过，可部署生产环境 |
| P2 | 1 周 | ~18 | — | 技术债清理，中高危非阻断缺陷修复。其中 S13（构造函数注入）约 1 人周 |
| P3 | 空闲期 | ~12 | — | 架构渐进式演进，合规补全。跨度大但不阻塞日常需求 |
| P4 | 远期 | ~5 | — | 需专门立项，跨团队协调 |

## 合并说明

本文件合并自：
- 原审计第七节（修复优先级与策略建议）— 覆盖 S1-S22 配置/架构缺陷的修复路线
- 八-F 节（更新后的修复优先级）— 基于四轮审计 57 项发现的完整优先级矩阵

合并原则：以八-F 为基线（更全面），将原第七节中仍在八-F 之外的项（S19 配置拆分、S22 Spring 版本）归入 P2-P3。时间估算取自审计结论："P0 修复预计 6 小时，P0+P1 全部准入条件预计 4-6 个工作日"。
