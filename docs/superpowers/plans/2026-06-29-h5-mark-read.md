# H5 通知标记已读 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用户打开 H5 通知面板时自动将所有反馈类通知标记为已读

**Architecture:** 后端在两个 mobile controller 各新增 `POST /alerts/read-all`，调用已有的 `StudentNotificationService.markAllRead()`。前端新增两个 API 函数，在 `openFeedback()` 中 fire-and-forget 调用。

**Tech Stack:** Java Spring Boot (后端), React TypeScript (前端)

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `src/.../student/controller/StudentMobileController.java` | Modify | JWT 路径 `POST /alerts/read-all` |
| `src/.../student/controller/StudentMobileCenterController.java` | Modify | Token 路径 `POST /{token}/alerts/read-all` |
| `frontend/src/api/domains/studentMobile.api.ts` | Modify | 🆕 `markStudentMobileAlertsReadAll()` |
| `frontend/src/api/domains/mobileStudent.api.ts` | Modify | 🆕 `markMobileAlertsReadAll(token)` |
| `frontend/src/pages/mobile/MobileStudentCenterPage.tsx` | Modify | `openFeedback()` 追加调用 |

---

### Task 1: 后端 — JWT 路径 batch read-all

**Files:**
- Modify: `src/main/java/com/example/demo/modules/student/controller/StudentMobileController.java`

- [ ] **Step 1: 检查 StudentNotificationService 是否已注入**

Read `StudentMobileController.java`，搜索 `StudentNotificationService`。如果不在已有的字段和构造函数参数中，需要添加。

```java
// 如果不存在，添加字段：
private final StudentNotificationService studentNotificationService;

// 并在构造函数参数列表末尾追加：
StudentNotificationService studentNotificationService
// 在构造函数体内赋值：
this.studentNotificationService = studentNotificationService;

// 添加 import：
import com.example.demo.modules.student.service.StudentNotificationService;
```

- [ ] **Step 2: 添加 POST /alerts/read-all 端点**

在 `getAlerts` 方法之后添加：

```java
    @PostMapping("/alerts/read-all")
    @Operation(summary = "将所有反馈类通知标记为已读（JWT）")
    public Result<Void> markAlertsReadAll(HttpServletRequest request) {
        User user = requireCurrentUser(request);
        studentNotificationService.markAllRead(user);
        return Result.success(null);
    }
```

- [ ] **Step 3: 编译验证**

```bash
cd d:/codex/verson.1.2/20260416 && ./mvnw compile -pl . -q
```

Expected: BUILD SUCCESS

- [ ] **Step 4: 提交**

```bash
git add src/main/java/com/example/demo/modules/student/controller/StudentMobileController.java
git commit -m "feat: add POST /student/mobile/alerts/read-all endpoint"
```

---

### Task 2: 后端 — Token 路径 batch read-all

**Files:**
- Modify: `src/main/java/com/example/demo/modules/student/controller/StudentMobileCenterController.java`

- [ ] **Step 1: 检查 StudentNotificationService 是否已注入**

同上。搜索 `StudentNotificationService`，如不存在则添加字段、构造函数参数和 import。

- [ ] **Step 2: 添加 POST /api/public/mobile-center/{token}/alerts/read-all**

在 `getMobileAlerts` 方法之后添加：

```java
    @PostMapping("/api/public/mobile-center/{token}/alerts/read-all")
    @Operation(summary = "将所有反馈类通知标记为已读（手机版 token）")
    public Result<Void> markAlertsReadAll(@PathVariable String token,
                                           HttpServletRequest request) {
        String clientIp = getClientIp(request);
        String userId = tokenService.validateToken(token, clientIp);
        User user = userMapper.findById(userId);
        if (user == null) {
            return Result.fail(404, "用户不存在");
        }
        studentNotificationService.markAllRead(user);
        return Result.success(null);
    }
```

- [ ] **Step 3: 编译验证**

```bash
cd d:/codex/verson.1.2/20260416 && ./mvnw compile -pl . -q
```

Expected: BUILD SUCCESS

- [ ] **Step 4: 提交**

```bash
git add src/main/java/com/example/demo/modules/student/controller/StudentMobileCenterController.java
git commit -m "feat: add POST /public/mobile-center/{token}/alerts/read-all endpoint"
```

---

### Task 3: 前端 — API 函数

**Files:**
- Modify: `frontend/src/api/domains/studentMobile.api.ts`
- Modify: `frontend/src/api/domains/mobileStudent.api.ts`

- [ ] **Step 1: JWT 版本 — markStudentMobileAlertsReadAll**

在 `studentMobile.api.ts` 末尾添加：

```typescript
// ======================== Mark Read ========================

export async function markStudentMobileAlertsReadAll(): Promise<void> {
  await authHttp.post(`/student/mobile/alerts/read-all`);
}
```

- [ ] **Step 2: Token 版本 — markMobileAlertsReadAll**

在 `mobileStudent.api.ts` 末尾添加：

```typescript
// ======================== 标记已读 ========================

/** 将所有反馈类通知标记为已读（公开 token 接口） */
export async function markMobileAlertsReadAll(token: string): Promise<void> {
  await publicHttp.post(`/public/mobile-center/${encodeURIComponent(token)}/alerts/read-all`);
}
```

- [ ] **Step 3: TypeScript 编译验证**

```bash
cd d:/codex/verson.1.2/20260416/frontend && npx tsc --noEmit
```

Expected: 无报错

- [ ] **Step 4: 提交**

```bash
git add frontend/src/api/domains/studentMobile.api.ts
git add frontend/src/api/domains/mobileStudent.api.ts
git commit -m "feat: add mark-all-read API functions for mobile alerts"
```

---

### Task 4: 前端 — openFeedback 调用点

**Files:**
- Modify: `frontend/src/pages/mobile/MobileStudentCenterPage.tsx`

- [ ] **Step 1: 添加导入**

在文件顶部现有 imports 区域添加：

```typescript
import { markStudentMobileAlertsReadAll } from "@/api/domains/studentMobile.api";
import { markMobileAlertsReadAll } from "@/api/domains/mobileStudent.api";
```

- [ ] **Step 2: 修改 openFeedback**

找到 `openFeedback` 回调（约第145行），修改为：

```typescript
  const openFeedback = useCallback(() => {
    setShowFeedback(true);
    void loadAlerts();
    // 后台标记所有反馈通知为已读（fire-and-forget）
    if (token) {
      markMobileAlertsReadAll(token).catch(() => {});
    } else {
      markStudentMobileAlertsReadAll().catch(() => {});
    }
  }, [loadAlerts, token]);
```

- [ ] **Step 3: TypeScript 编译验证**

```bash
cd d:/codex/verson.1.2/20260416/frontend && npx tsc --noEmit
```

Expected: 无报错

- [ ] **Step 4: 提交**

```bash
git add frontend/src/pages/mobile/MobileStudentCenterPage.tsx
git commit -m "feat: call mark-all-read when opening feedback panel"
```

---

### Task 5: 端到端验证

- [ ] **Step 1: 编译后端**

```bash
./mvnw compile -pl . -q
```

- [ ] **Step 2: 编译前端**

```bash
cd frontend && npx tsc --noEmit
```

- [ ] **Step 3: 手动验证**

1. 启动应用，以有未读通知的用户登录 H5
2. 查看首页「通知」按钮角标数字
3. 点击「通知」打开反馈面板
4. 检查 Network 面板确认 `POST /alerts/read-all` 调用成功 (200)
5. 刷新页面，角标应消失（因为 `isRead=true`）

---

## Self-Review Checklist

- [x] Spec coverage: 两个后端接口 + 两个前端函数 + 一个调用点全部覆盖
- [x] Placeholder scan: 无 TBD/TODO
- [x] Type consistency: 前后端函数名一致，路径一致
