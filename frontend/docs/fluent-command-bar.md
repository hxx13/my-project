# Fluent 命令栏 / 密度（轻量落地）

1. **从左到右**：主任务（搜索、筛选）→ 次操作（导出、刷新）→ 分页或元信息。
2. **收纳**：超过一行的按钮组使用 `flex-wrap`；非关键操作可收进二级菜单（后续可增强）。
3. **触控目标**：按钮 `py-2` 及以上；`--admin-control-height` 作为表单控件参考高度。
4. **响应式**：窄屏下搜索框 `min-w-0 flex-1`，避免把按钮挤出视口。
