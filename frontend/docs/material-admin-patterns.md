# Material Design（管理端导航与表格）对照清单

1. **导航**：当前路由高亮（侧栏 `NavLink`）；分组可折叠，命中路由时自动展开分组。
2. **密度**：工具栏控件垂直对齐 `items-end`；表格区与筛选区分卡片或边框分隔。
3. **表头**：`thead` 语义 + 吸顶时保持 `z-index` 低于全局命令面板。
4. **行状态**：hover 背景区分；禁用行降低对比度。
5. **空状态**：无数据时简短说明 + 可选「重试」/「调整筛选」。
