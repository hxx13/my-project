import AdminScheduleManagerPage from "@/pages/AdminScheduleManagerPage";

/**
 * SchedulerSettings sub-page for AdminSettingsLayout.
 * Wraps the standalone AdminScheduleManagerPage — minor visual nesting
 * from the outer AdminPageShell is acceptable.
 */
export default function SchedulerSettings() {
  return <AdminScheduleManagerPage />;
}
