# ===== 生产环境配置 =====
# 通过 SPRING_PROFILES_ACTIVE=prod 自动加载（覆盖 application.properties）

# ── 数据库 ──
spring.datasource.password=${DB_PASSWORD}

# ── 文件存储根路径 ──
app.data.base-dir=/opt/twin/data

# ── 各模块存储路径 ──
app.chat.upload-dir=/opt/twin/data/chat-uploads
app.admin.template-upload-dir=/opt/twin/data/admin-template-uploads
app.speech.storage-dir=/opt/twin/data/speech
app.login-branding.upload-dir=/opt/twin/data/login-branding
app.upload.base-dir=/opt/twin/data


app.public-base-url=https://aroultra.shsmu.edu.cn
