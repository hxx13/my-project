-- 与 common/schema/V20260821003__obligation_backfill_violations.sql 对齐
-- 注意：全量回填可能较重，默认不在 EmbeddedTwinSystemCoreDdlBootstrap 自动执行；
-- 请用管理端 POST /api/admin/twin/obligations/backfill-violations 分批跑。

SELECT 1;
