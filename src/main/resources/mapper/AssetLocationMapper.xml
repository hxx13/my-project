<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.asset.mapper.AssetLocationMapper">

    <resultMap id="AssetLocationResultMap" type="com.example.demo.modules.asset.entity.AssetLocation">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="deleted" column="deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="listAll" resultMap="AssetLocationResultMap">
        SELECT id, parent_id, name, sort_order, deleted, created_at, updated_at
        FROM asset_location
        WHERE deleted = 0
        ORDER BY sort_order, id
    </select>

    <select id="findById" resultMap="AssetLocationResultMap">
        SELECT id, parent_id, name, sort_order, deleted, created_at, updated_at
        FROM asset_location
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="findByIds" resultMap="AssetLocationResultMap">
        SELECT id, parent_id, name, sort_order, deleted, created_at, updated_at
        FROM asset_location
        WHERE deleted = 0 AND id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <insert id="insert" parameterType="com.example.demo.modules.asset.entity.AssetLocation"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO asset_location(parent_id, name, sort_order)
        VALUES (#{parentId}, #{name}, #{sortOrder})
    </insert>

    <update id="updateNode">
        UPDATE asset_location
        SET name = #{name},
            parent_id = #{parentId},
            sort_order = #{sortOrder},
            updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="softDelete">
        UPDATE asset_location
        SET deleted = 1, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="countChildren" resultType="int">
        SELECT COUNT(1) FROM asset_location WHERE parent_id = #{id} AND deleted = 0
    </select>

    <select id="countAssets" resultType="int">
        SELECT COUNT(1) FROM asset_record WHERE location_node_id = #{id} AND deleted = 0
    </select>

    <select id="countAssetsGroupByNode" resultType="java.util.Map">
        SELECT location_node_id AS nodeId, COUNT(1) AS cnt
        FROM asset_record
        WHERE deleted = 0 AND location_node_id IS NOT NULL
        GROUP BY location_node_id
    </select>
</mapper>
