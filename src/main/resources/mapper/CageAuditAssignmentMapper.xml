<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.cageshelf.mapper.CageAuditAssignmentMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cage_audit_assignment (reviewer_user_id, scope_type, scope_id)
        VALUES (#{reviewerUserId}, #{scopeType}, #{scopeId})
    </insert>

    <delete id="deleteByReviewer">
        DELETE FROM cage_audit_assignment WHERE reviewer_user_id = #{reviewerUserId}
    </delete>

    <select id="listByReviewer" resultType="com.example.demo.modules.cageshelf.entity.CageAuditAssignment">
        SELECT * FROM cage_audit_assignment WHERE reviewer_user_id = #{reviewerUserId}
        ORDER BY scope_type ASC, scope_id ASC
    </select>

    <select id="listAll" resultType="com.example.demo.modules.cageshelf.entity.CageAuditAssignment">
        SELECT * FROM cage_audit_assignment
        ORDER BY reviewer_user_id ASC, scope_type ASC, scope_id ASC
    </select>

</mapper>
