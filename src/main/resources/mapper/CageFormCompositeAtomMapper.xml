<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.cageshelf.mapper.CageFormCompositeAtomMapper">

    <resultMap id="CageFormCompositeAtomMap" type="com.example.demo.modules.cageshelf.entity.CageFormCompositeAtom">
        <id property="id" column="id"/>
        <result property="compositeTemplateId" column="composite_template_id"/>
        <result property="atomTemplateId" column="atom_template_id"/>
        <result property="atomCode" column="atom_code"/>
        <result property="sortOrder" column="sort_order"/>
    </resultMap>

    <select id="selectByCompositeId" resultMap="CageFormCompositeAtomMap">
        SELECT id, composite_template_id, atom_template_id, atom_code, sort_order
        FROM cage_form_composite_atom
        WHERE composite_template_id = #{compositeTemplateId}
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectByAtomId" resultMap="CageFormCompositeAtomMap">
        SELECT id, composite_template_id, atom_template_id, atom_code, sort_order
        FROM cage_form_composite_atom
        WHERE atom_template_id = #{atomTemplateId}
        ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cage_form_composite_atom (composite_template_id, atom_template_id, atom_code, sort_order)
        VALUES (#{compositeTemplateId}, #{atomTemplateId}, #{atomCode}, #{sortOrder})
    </insert>

    <delete id="deleteByCompositeId">
        DELETE FROM cage_form_composite_atom WHERE composite_template_id = #{compositeTemplateId}
    </delete>
</mapper>
