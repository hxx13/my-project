<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.cageshelf.mapper.CagePermissionMapper">

    <select id="listCapabilities" resultType="com.example.demo.modules.cageshelf.entity.CagePermissionCapability">
        SELECT * FROM cage_permission_capability WHERE active = 1
        ORDER BY sort_order ASC, code ASC
    </select>

    <select id="listGrants" resultType="com.example.demo.modules.cageshelf.entity.CagePermissionGrant">
        SELECT * FROM cage_permission_grant
        ORDER BY capability_code ASC, identity_code ASC
    </select>

    <insert id="insertGrant" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO cage_permission_grant (capability_code, identity_code)
        VALUES (#{capabilityCode}, #{identityCode})
    </insert>

    <delete id="deleteGrant">
        DELETE FROM cage_permission_grant
        WHERE capability_code = #{capabilityCode} AND identity_code = #{identityCode}
    </delete>

    <!-- 组员级勾选（组长逐人勾）：user_id = personnel.id -->
    <select id="listMemberCapabilities" resultType="com.example.demo.modules.cageshelf.entity.CageMemberCapability">
        SELECT * FROM cage_member_capability WHERE user_id = #{userId}
        ORDER BY capability_code ASC
    </select>

    <delete id="deleteMemberCapabilities">
        DELETE FROM cage_member_capability WHERE user_id = #{userId}
    </delete>

    <insert id="insertMemberCapability" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO cage_member_capability (user_id, capability_code, granted_by)
        VALUES (#{userId}, #{capabilityCode}, #{grantedBy})
    </insert>

</mapper>
