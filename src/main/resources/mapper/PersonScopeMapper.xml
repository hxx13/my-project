<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.identity.mapper.PersonScopeMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO person_scope (user_id, scope_type, scope_id)
        VALUES (#{userId}, #{scopeType}, #{scopeId})
    </insert>

    <delete id="deleteByUser">
        DELETE FROM person_scope WHERE user_id = #{userId}
    </delete>

    <select id="listByUser" resultType="com.example.demo.modules.identity.entity.PersonScope">
        SELECT * FROM person_scope WHERE user_id = #{userId}
        ORDER BY scope_type ASC, scope_id ASC
    </select>

    <select id="listAll" resultType="com.example.demo.modules.identity.entity.PersonScope">
        SELECT * FROM person_scope
        ORDER BY user_id ASC, scope_type ASC, scope_id ASC
    </select>

</mapper>
