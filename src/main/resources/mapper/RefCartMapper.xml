<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.referencedata.mapper.RefCartMapper">

    <resultMap id="Base" type="com.example.demo.modules.referencedata.entity.RefCart">
        <id property="id" column="id"/>
        <result property="groupId" column="group_id"/>
        <result property="refDataId" column="ref_data_id"/>
        <result property="aupRecordId" column="aup_record_id"/>
        <result property="specSelections" column="spec_selections"/>
        <result property="quantity" column="quantity"/>
        <result property="pickupRoomId" column="pickup_room_id"/>
        <result property="pickupRoomName" column="pickup_room_name"/>
        <result property="collectorId" column="collector_id"/>
        <result property="collectorName" column="collector_name"/>
        <result property="editingOrderId" column="editing_order_id"/>
        <result property="remark" column="remark"/>
        <result property="packageStatus" column="package_status"/>
        <result property="packageRemark" column="package_remark"/>
        <result property="targetAnimalCageId" column="target_animal_cage_id"/>
        <result property="addedBy" column="added_by"/>
        <result property="addedAt" column="added_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ref_cart(group_id, ref_data_id, aup_record_id, spec_selections, quantity,
                             pickup_room_id, pickup_room_name, collector_id, collector_name, editing_order_id, remark,
                             package_status, package_remark, target_animal_cage_id, added_by)
        VALUES (#{groupId}, #{refDataId}, #{aupRecordId}, #{specSelections}, #{quantity},
                #{pickupRoomId}, #{pickupRoomName}, #{collectorId}, #{collectorName}, #{editingOrderId}, #{remark},
                COALESCE(#{packageStatus}, 'DRAFT'), #{packageRemark}, #{targetAnimalCageId}, #{addedBy})
    </insert>

    <update id="update">
        UPDATE ref_cart
        SET spec_selections = #{specSelections},
            quantity = #{quantity},
            remark = #{remark},
            package_status = COALESCE(#{packageStatus}, package_status),
            package_remark = #{packageRemark},
            pickup_room_id = COALESCE(#{pickupRoomId}, pickup_room_id),
            pickup_room_name = COALESCE(#{pickupRoomName}, pickup_room_name),
            collector_id = COALESCE(#{collectorId}, collector_id),
            collector_name = COALESCE(#{collectorName}, collector_name),
            aup_record_id = COALESCE(#{aupRecordId}, aup_record_id)
        WHERE id = #{id}
    </update>

    <update id="updatePackageStatus">
        UPDATE ref_cart
        SET package_status = #{packageStatus},
            package_remark = #{packageRemark}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ref_cart WHERE id = #{id}
    </delete>

    <delete id="deleteByGroupId">
        DELETE FROM ref_cart WHERE group_id = #{groupId}
    </delete>

    <!-- 编辑在途：按订单精确定位回填行，保存/放弃/重入都靠它，不误伤用户其它购物车内容 -->
    <select id="listByEditingOrderId" resultMap="Base">
        SELECT * FROM ref_cart WHERE editing_order_id = #{orderId} ORDER BY id ASC
    </select>

    <delete id="deleteByEditingOrderId">
        DELETE FROM ref_cart WHERE editing_order_id = #{orderId}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM ref_cart
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findById" resultMap="Base">
        SELECT * FROM ref_cart WHERE id = #{id} LIMIT 1
    </select>

    <select id="listByGroupId" resultMap="Base">
        SELECT * FROM ref_cart WHERE group_id = #{groupId} ORDER BY id ASC
    </select>

    <select id="listByIds" resultMap="Base">
        SELECT * FROM ref_cart
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY id ASC
    </select>

</mapper>
