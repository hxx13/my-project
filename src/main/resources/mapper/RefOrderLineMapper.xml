<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.referencedata.mapper.RefOrderLineMapper">

    <resultMap id="Base" type="com.example.demo.modules.referencedata.entity.RefOrderLine">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="refDataId" column="ref_data_id"/>
        <result property="supplierName" column="supplier_name"/>
        <result property="strainName" column="strain_name"/>
        <result property="specName" column="spec_name"/>
        <result property="specSelections" column="spec_selections"/>
        <result property="hierarchyChain" column="hierarchy_chain"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="arrivalDate" column="arrival_date"/>
        <result property="pickupRoomId" column="pickup_room_id"/>
        <result property="pickupRoomName" column="pickup_room_name"/>
        <result property="collectorId" column="collector_id"/>
        <result property="collectorName" column="collector_name"/>
        <result property="lineRemark" column="line_remark"/>
        <result property="addedBy" column="added_by"/>
        <result property="aupRecordId" column="aup_record_id"/>
        <result property="targetAnimalCageId" column="target_animal_cage_id"/>
        <result property="targetCageLocation" column="target_cage_location"/>
        <result property="pickupMode" column="pickup_mode"/>
        <result property="deliveryCycle" column="delivery_cycle"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ref_order_line(order_id, ref_data_id, supplier_name, strain_name, spec_name,
                                   spec_selections, hierarchy_chain, quantity, unit_price, arrival_date,
                                   pickup_room_id, pickup_room_name, collector_id, collector_name,
                                   line_remark, added_by, aup_record_id, target_animal_cage_id, target_cage_location,
                                   pickup_mode, delivery_cycle)
        VALUES (#{orderId}, #{refDataId}, #{supplierName}, #{strainName}, #{specName},
                #{specSelections}, #{hierarchyChain}, #{quantity}, #{unitPrice}, #{arrivalDate},
                #{pickupRoomId}, #{pickupRoomName}, #{collectorId}, #{collectorName},
                #{lineRemark}, #{addedBy}, #{aupRecordId}, #{targetAnimalCageId}, #{targetCageLocation},
                COALESCE(#{pickupMode}, 'FARM'), #{deliveryCycle})
    </insert>

    <delete id="deleteByOrderId">
        DELETE FROM ref_order_line WHERE order_id = #{orderId}
    </delete>

    <select id="listByOrderId" resultMap="Base">
        SELECT id, order_id, ref_data_id, supplier_name, strain_name, spec_name,
               spec_selections, hierarchy_chain, quantity, unit_price, arrival_date,
               pickup_room_id, pickup_room_name, collector_id, collector_name,
               line_remark, added_by, aup_record_id, target_animal_cage_id, target_cage_location, pickup_mode,
               delivery_cycle
        FROM ref_order_line WHERE order_id = #{orderId} ORDER BY id ASC
    </select>

    <!-- 周期库存占用（已下单侧）：返回数量 + 订单状态，REJECTED/CANCELLED 是否计入由服务层过滤。 -->
    <select id="listCycleUsage" resultType="com.example.demo.modules.referencedata.dto.OrderLineCycleUsage">
        SELECT rl.quantity AS quantity, o.status AS orderStatus
        FROM ref_order_line rl
        INNER JOIN ref_order o ON o.id = rl.order_id
        WHERE rl.ref_data_id = #{refDataId}
          AND rl.delivery_cycle = #{cycle}
        <if test="specOption != null and specOption != ''">
            AND JSON_UNQUOTE(JSON_EXTRACT(rl.spec_selections, '$.option')) = #{specOption}
        </if>
    </select>

</mapper>
