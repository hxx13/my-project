<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.inventory.mapper.SpaceMapper">
    <select id="selectAll" resultType="com.example.demo.modules.inventory.entity.InvSpace">
        SELECT * FROM inv_space WHERE deleted = 0 ORDER BY sort_order ASC, id ASC
    </select>
    <select id="selectById" resultType="com.example.demo.modules.inventory.entity.InvSpace">
        SELECT * FROM inv_space WHERE id = #{id} AND deleted = 0
    </select>
    <select id="selectByCode" resultType="com.example.demo.modules.inventory.entity.InvSpace">
        SELECT * FROM inv_space WHERE code = #{code} AND deleted = 0 LIMIT 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inv_space (parent_id, name, type, icon, pos_x, pos_y, width, height, sort_order, code, deleted)
        VALUES (#{parentId}, #{name}, #{type}, #{icon}, #{posX}, #{posY}, #{width}, #{height}, #{sortOrder}, #{code}, 0)
    </insert>
    <update id="updateById">
        UPDATE inv_space
        SET parent_id = #{parentId}, name = #{name}, type = #{type}, icon = #{icon},
            pos_x = #{posX}, pos_y = #{posY}, width = #{width}, height = #{height},
            sort_order = #{sortOrder}, code = #{code}
        WHERE id = #{id}
    </update>
    <update id="softDelete">
        UPDATE inv_space SET deleted = 1 WHERE id = #{id}
    </update>
    <select id="countChildren" resultType="int">
        SELECT COUNT(1) FROM inv_space WHERE parent_id = #{parentId} AND deleted = 0
    </select>
    <select id="countItemsInSpace" resultType="int">
        SELECT COUNT(1) FROM inv_item WHERE space_id = #{spaceId} AND deleted = 0
    </select>
</mapper>
