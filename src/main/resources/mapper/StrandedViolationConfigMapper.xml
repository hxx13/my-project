<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.twin.dashboard.mapper.StrandedViolationConfigMapper">

    <select id="selectConfig" resultType="map">
        SELECT *
        FROM stranded_violation_config
        WHERE id = 1
    </select>

    <select id="selectSignoutConfig" resultType="map">
        SELECT *
        FROM stranded_violation_config
        WHERE id = 2
    </select>

    <update id="updateExecutionResult">
        UPDATE stranded_violation_config
        SET last_execution_at = #{executedAt},
            last_execution_result = #{result}
        WHERE id = 1
    </update>

    <update id="updateSignoutExecutionResult">
        UPDATE stranded_violation_config
        SET last_execution_at = #{executedAt},
            last_execution_result = #{result}
        WHERE id = 2
    </update>

    <update id="updateSignoutOnlyConfig">
        UPDATE stranded_violation_config
        SET auto_signout_enabled = #{autoSignoutEnabled}
        WHERE id = 2
    </update>

    <insert id="ensureSignoutConfigRow">
        INSERT INTO stranded_violation_config (id, enabled, auto_signout_enabled)
        VALUES (2, 0, 1)
        ON DUPLICATE KEY UPDATE id = id
    </insert>

    <update id="updateConfig">
        UPDATE stranded_violation_config
        SET auto_signout_enabled = #{autoSignoutEnabled},
            violation_text_tpl = #{violationTextTpl},
            forbid_enter = #{forbidEnter},
            expire_after_days = #{expireAfterDays},
            whitelist_depts = #{whitelistDepts},
            interactive_challenge_enabled = #{interactiveChallengeEnabled},
            interactive_challenge_phrase = #{interactiveChallengePhrase},
            interactive_unlock_on_verify = #{interactiveUnlockOnVerify}
        WHERE id = 1
    </update>
</mapper>
