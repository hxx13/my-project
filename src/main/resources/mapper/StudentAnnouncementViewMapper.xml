<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.student.mapper.StudentAnnouncementViewMapper">

    <select id="selectLastViewedAt" resultType="java.time.LocalDateTime">
        SELECT last_viewed_at
        FROM student_announcement_view
        WHERE user_id = #{userId}
    </select>

    <insert id="upsertLastViewedAt">
        INSERT INTO student_announcement_view (user_id, last_viewed_at)
        VALUES (#{userId}, NOW(3))
        ON DUPLICATE KEY UPDATE last_viewed_at = NOW(3)
    </insert>
</mapper>
