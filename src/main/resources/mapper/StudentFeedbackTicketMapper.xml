<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.student.mapper.StudentFeedbackTicketMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.modules.student.entity.StudentFeedbackTicket">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="reply_content" property="replyContent"/>
        <result column="replied_by" property="repliedBy"/>
        <result column="replied_at" property="repliedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student_feedback_ticket (user_id, subject, content, type, status, created_at, updated_at)
        VALUES (#{userId}, #{subject}, #{content}, #{type}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM student_feedback_ticket WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM student_feedback_ticket WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM student_feedback_ticket WHERE user_id = #{userId}
    </select>
</mapper>
