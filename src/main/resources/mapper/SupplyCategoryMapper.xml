<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.supplies.mapper.SupplyCategoryMapper">

    <resultMap id="Base" type="com.example.demo.modules.supplies.entity.SupplyCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supply_category(name, sort_order, status)
        VALUES (#{name}, #{sortOrder}, #{status})
    </insert>

    <update id="update">
        UPDATE supply_category
        SET name = #{name},
            sort_order = #{sortOrder},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM supply_category WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="Base">
        SELECT * FROM supply_category WHERE id = #{id} LIMIT 1
    </select>

    <select id="listEnabledOrdered" resultMap="Base">
        SELECT * FROM supply_category WHERE status = 1 ORDER BY sort_order ASC, id ASC
    </select>

    <select id="listAllOrdered" resultMap="Base">
        SELECT * FROM supply_category ORDER BY sort_order ASC, id ASC
    </select>
</mapper>
