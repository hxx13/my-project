<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.supplies.mapper.SupplyUserViewStateMapper">

    <select id="findLastViewedAt" resultType="java.time.LocalDateTime">
        SELECT last_viewed_at
        FROM supply_user_view_state
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="upsertLastViewedAt">
        INSERT INTO supply_user_view_state(user_id, last_viewed_at)
        VALUES (#{userId}, #{lastViewedAt})
        ON DUPLICATE KEY UPDATE last_viewed_at = VALUES(last_viewed_at)
    </insert>
</mapper>
