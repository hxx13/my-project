<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.telemetry.mapper.TelemetryChartGroupMapper">

    <resultMap id="ChartGroupMap" type="com.example.demo.modules.telemetry.entity.TelemetryChartGroupRow">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="variable_names_json" property="variableNamesJson"/>
        <result column="variable_metadata_json" property="variableMetadataJson"/>
        <result column="layout_mode" property="layoutMode"/>
        <result column="source" property="source"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="ChartGroupMap">
        SELECT id, name, description, variable_names_json, variable_metadata_json, layout_mode, source, sort_order, create_time, update_time
        FROM telemetry_chart_group
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectById" resultMap="ChartGroupMap">
        SELECT id, name, description, variable_names_json, variable_metadata_json, layout_mode, source, sort_order, create_time, update_time
        FROM telemetry_chart_group WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO telemetry_chart_group (
            name, description, variable_names_json, variable_metadata_json, layout_mode, source, sort_order
        ) VALUES (
            #{name}, #{description}, #{variableNamesJson}, #{variableMetadataJson}, #{layoutMode}, #{source}, #{sortOrder}
        )
    </insert>

    <update id="update">
        UPDATE telemetry_chart_group SET
            name = #{name},
            description = #{description},
            variable_names_json = #{variableNamesJson},
            variable_metadata_json = #{variableMetadataJson},
            layout_mode = #{layoutMode},
            source = #{source},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM telemetry_chart_group WHERE id = #{id}
    </delete>
</mapper>
