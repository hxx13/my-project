<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.twin.obligation.mapper.TwinObligationMapper">

    <resultMap id="TwinObligationMap" type="com.example.demo.modules.twin.obligation.entity.TwinObligation">
        <id column="id" property="id"/>
        <result column="subject_user_id" property="subjectUserId"/>
        <result column="source_type" property="sourceType"/>
        <result column="source_id" property="sourceId"/>
        <result column="title" property="title"/>
        <result column="content_html" property="contentHtml"/>
        <result column="content_json" property="contentJson"/>
        <result column="disposition_type" property="dispositionType"/>
        <result column="disposition_config_json" property="dispositionConfigJson"/>
        <result column="require_reconfirm" property="requireReconfirm"/>
        <result column="status" property="status"/>
        <result column="due_at" property="dueAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO twin_obligation (
            subject_user_id, source_type, source_id, title, content_html, content_json,
            disposition_type, disposition_config_json, status, due_at
        ) VALUES (
            #{subjectUserId}, #{sourceType}, #{sourceId}, #{title}, #{contentHtml}, #{contentJson},
            #{dispositionType}, #{dispositionConfigJson}, #{status}, #{dueAt}
        )
    </insert>

    <select id="selectBySource" resultMap="TwinObligationMap">
        SELECT * FROM twin_obligation
        WHERE source_type = #{sourceType} AND source_id = #{sourceId}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="TwinObligationMap">
        SELECT * FROM twin_obligation WHERE id = #{id}
    </select>

    <select id="selectBySubject" resultMap="TwinObligationMap">
        SELECT * FROM twin_obligation
        WHERE subject_user_id = #{subjectUserId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <select id="selectAdmin" resultMap="TwinObligationMap">
        SELECT * FROM twin_obligation
        WHERE 1=1
        <if test="subjectUserId != null and subjectUserId != ''">
            AND subject_user_id = #{subjectUserId}
        </if>
        <if test="sourceType != null and sourceType != ''">
            AND source_type = #{sourceType}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <update id="updateStatus">
        UPDATE twin_obligation
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateContentAndDue">
        UPDATE twin_obligation
        SET title = #{title},
            content_html = #{contentHtml},
            content_json = #{contentJson},
            disposition_type = #{dispositionType},
            disposition_config_json = #{dispositionConfigJson},
            due_at = #{dueAt},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 存量 ACTIVE 违规 → obligation；已存在 source 则跳过（INSERT IGNORE + 唯一键） -->
    <insert id="backfillFromActiveViolations">
        INSERT IGNORE INTO twin_obligation (
            subject_user_id, source_type, source_id, title, content_html,
            disposition_type, disposition_config_json, status, due_at
        )
        SELECT
            v.target_user_id,
            'STUDENT_VIOLATION',
            CAST(v.id AS CHAR),
            CASE
                WHEN v.interactive_challenge IS NOT NULL AND TRIM(v.interactive_challenge) &lt;&gt; ''
                    THEN '违规交互确认'
                ELSE '违规提醒'
            END,
            v.violation_text,
            CASE
                WHEN v.interactive_challenge IS NOT NULL AND TRIM(v.interactive_challenge) &lt;&gt; ''
                    THEN 'ACK_PUZZLE'
                ELSE 'SHOW_ONLY'
            END,
            CASE
                WHEN v.interactive_challenge IS NOT NULL AND TRIM(v.interactive_challenge) &lt;&gt; ''
                    THEN JSON_OBJECT('phrase', v.interactive_challenge)
                ELSE NULL
            END,
            CASE
                WHEN v.interactive_challenge_verified_at IS NOT NULL THEN 'COMPLETED'
                ELSE 'PENDING_DISPOSITION'
            END,
            v.expire_at
        FROM twin_student_violation v
        WHERE v.status = 'ACTIVE'
          AND v.target_user_id IS NOT NULL
          AND TRIM(v.target_user_id) &lt;&gt; ''
        ORDER BY v.id ASC
        LIMIT #{limit}
    </insert>

    <update id="expireOverdue">
        UPDATE twin_obligation
        SET status = 'EXPIRED', updated_at = NOW()
        WHERE due_at IS NOT NULL
          AND due_at &lt; NOW()
          AND status NOT IN ('COMPLETED', 'EXPIRED', 'REVOKED')
        ORDER BY due_at ASC
        LIMIT #{limit}
    </update>
</mapper>
