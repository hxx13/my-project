<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.twin.dashboard.mapper.TwinStudentViolationMapper">

    <resultMap id="TwinStudentViolationMap" type="com.example.demo.modules.twin.dashboard.entity.TwinStudentViolation">
        <id column="id" property="id"/>
        <result column="target_user_id" property="targetUserId"/>
        <result column="violation_text" property="violationText"/>
        <result column="content_json" property="contentJson"/>
        <result column="image_urls" property="imageUrls"/>
        <result column="forbid_enter" property="forbidEnter"/>
        <result column="max_enter_success" property="maxEnterSuccess"/>
        <result column="enter_success_count" property="enterSuccessCount"/>
        <result column="show_notice_every_scan" property="showNoticeEveryScan"/>
        <result column="expire_at" property="expireAt"/>
        <result column="status" property="status"/>
        <result column="created_by_user_id" property="createdByUserId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="cleared_at" property="clearedAt"/>
        <result column="cleared_by_user_id" property="clearedByUserId"/>
        <result column="source" property="source"/>
        <result column="interactive_challenge" property="interactiveChallenge"/>
        <result column="interactive_challenge_verified_at" property="interactiveChallengeVerifiedAt"/>
        <result column="interactive_unlock_on_verify" property="interactiveUnlockOnVerify"/>
        <result column="rule_id" property="ruleId"/>
        <result column="cage_violation_id" property="cageViolationId"/>
        <result column="batch_id" property="batchId"/>
        <result column="notice_display_days" property="noticeDisplayDays"/>
        <result column="notice_link_expire" property="noticeLinkExpire"/>
        <result column="notice_cleared_at" property="noticeClearedAt"/>
        <result column="notice_cleared_by_user_id" property="noticeClearedByUserId"/>
    </resultMap>

    <!-- 交互验证未完成时，超期仍保持 ACTIVE，直至用户完成拼图 -->
    <sql id="pendingInteractivePastExpireClause">
        interactive_challenge IS NOT NULL
        AND TRIM(interactive_challenge) &lt;&gt; ''
        AND interactive_challenge_verified_at IS NULL
    </sql>

    <update id="expireActivePastDue">
        UPDATE twin_student_violation
        SET status = 'EXPIRED', updated_at = NOW()
        WHERE status = 'ACTIVE'
          AND expire_at IS NOT NULL
          AND expire_at &lt; NOW()
          AND NOT (<include refid="pendingInteractivePastExpireClause"/>)
    </update>

    <select id="selectIdsDueToExpire" resultType="long">
        SELECT id
        FROM twin_student_violation
        WHERE status = 'ACTIVE'
          AND expire_at IS NOT NULL
          AND expire_at &lt; NOW()
          AND NOT (<include refid="pendingInteractivePastExpireClause"/>)
    </select>

    <update id="expireByIdIfPastDue">
        UPDATE twin_student_violation
        SET status = 'EXPIRED', updated_at = NOW()
        WHERE id = #{id}
          AND status = 'ACTIVE'
          AND expire_at IS NOT NULL
          AND expire_at &lt; NOW()
    </update>

    <select id="selectActiveIdsByTargetUserId" resultType="long">
        SELECT id
        FROM twin_student_violation
        WHERE target_user_id = #{targetUserId}
          AND status = 'ACTIVE'
    </select>

    <update id="supersedeActiveByTargetUserId">
        UPDATE twin_student_violation
        SET status = 'SUPERSEDED',
            cleared_at = NOW(),
            cleared_by_user_id = 'SYSTEM',
            updated_at = NOW()
        WHERE target_user_id = #{targetUserId}
          AND status = 'ACTIVE'
    </update>

    <select id="selectActiveByTargetUserId" resultMap="TwinStudentViolationMap">
        <!-- T2-1：enter_success_count 取物理列（进入次数），禁止用违规条数子查询覆盖 -->
        SELECT v.*
        FROM twin_student_violation v
        WHERE v.target_user_id = #{targetUserId}
          AND v.status = 'ACTIVE'
          AND (
            v.expire_at IS NULL
            OR v.expire_at &gt; NOW()
            OR (<include refid="pendingInteractivePastExpireClause"/>)
          )
        ORDER BY v.id DESC
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO twin_student_violation (
            target_user_id, violation_text, content_json, image_urls,
            forbid_enter, max_enter_success, enter_success_count, show_notice_every_scan,
            expire_at, status, created_by_user_id, source, interactive_challenge, interactive_unlock_on_verify,
            rule_id, cage_violation_id, batch_id,
            notice_display_days, notice_link_expire, notice_cleared_at, notice_cleared_by_user_id
        ) VALUES (
            #{targetUserId}, #{violationText}, #{contentJson}, #{imageUrls},
            #{forbidEnter}, #{maxEnterSuccess}, #{enterSuccessCount}, #{showNoticeEveryScan},
            #{expireAt}, #{status}, #{createdByUserId}, #{source}, #{interactiveChallenge}, #{interactiveUnlockOnVerify},
            #{ruleId}, #{cageViolationId}, #{batchId},
            #{noticeDisplayDays}, #{noticeLinkExpire}, #{noticeClearedAt}, #{noticeClearedByUserId}
        )
    </insert>

    <update id="updateClearById">
        UPDATE twin_student_violation
        SET status = 'CLEARED',
            cleared_at = NOW(),
            cleared_by_user_id = #{clearedByUserId},
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'ACTIVE'
    </update>

    <update id="markProcessedById">
        UPDATE twin_student_violation
        SET status = 'PROCESSED',
            cleared_at = NOW(),
            cleared_by_user_id = #{operatorUserId},
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'ACTIVE'
    </update>

    <update id="incrementEnterSuccess">
        UPDATE twin_student_violation
        SET enter_success_count = enter_success_count + 1,
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'ACTIVE'
    </update>

    <sql id="recentFilter">
        <where>
            <if test="targetUserId != null and targetUserId != ''">
                AND v.target_user_id = #{targetUserId}
            </if>
            <if test="statuses != null and statuses.size() > 0">
                AND v.status IN
                <foreach collection="statuses" item="s" open="(" separator="," close=")">
                    #{s}
                </foreach>
            </if>
            <if test="sources != null and sources.size() > 0">
                AND v.source IN
                <foreach collection="sources" item="src" open="(" separator="," close=")">
                    #{src}
                </foreach>
            </if>
            <!-- lockedOnly 三元：null=不过滤 / true=仅禁入 / false=仅可进入；须与前端 violationEnterLocked 一致 -->
            <if test="lockedOnly != null">
                AND (
                    <choose>
                        <when test="lockedOnly">
                            v.status = 'ACTIVE'
                            AND (
                                v.forbid_enter = 1
                                OR (v.interactive_challenge IS NOT NULL AND TRIM(v.interactive_challenge) &lt;&gt; '' AND v.interactive_challenge_verified_at IS NULL)
                                OR (v.max_enter_success IS NOT NULL AND v.enter_success_count &gt;= v.max_enter_success)
                            )
                        </when>
                        <otherwise>
                            NOT (
                                v.status = 'ACTIVE'
                                AND (
                                    v.forbid_enter = 1
                                    OR (v.interactive_challenge IS NOT NULL AND TRIM(v.interactive_challenge) &lt;&gt; '' AND v.interactive_challenge_verified_at IS NULL)
                                    OR (v.max_enter_success IS NOT NULL AND v.enter_success_count &gt;= v.max_enter_success)
                                )
                            )
                        </otherwise>
                    </choose>
                )
            </if>
        </where>
    </sql>

    <select id="selectRecent" resultMap="TwinStudentViolationMap">
        SELECT v.*
        FROM twin_student_violation v
        <include refid="recentFilter"/>
        <!-- 批次倒序、批内正序；batch_id 为 NULL 的历史行在 DESC 下排最后（沉底） -->
        ORDER BY v.batch_id DESC, v.id ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countRecent" resultType="int">
        SELECT COUNT(1)
        FROM twin_student_violation v
        <include refid="recentFilter"/>
    </select>

    <!-- 大屏公示严格按到期时间过滤：拼图未完成者超期后扫码端仍弹窗（selectActiveByTargetUserId），
         但已下榜，不再出现在主页「提醒公示」 -->
    <!--
      大屏公告可见性口径（唯一来源，两处查询共用，禁止各自改写）：
      ACTIVE + 公告未被单独解除 + 仍在公告展示窗口内。
      联动(notice_link_expire=1)展示到到期时间；否则展示到 created_at + notice_display_days 天。
      历史行 notice_link_expire 默认 1，故行为与加此列前一致。
    -->
    <sql id="boardVisibleClause">
        status = 'ACTIVE'
        AND notice_cleared_at IS NULL
        AND (
          CASE WHEN notice_link_expire = 1
               THEN (expire_at IS NULL OR expire_at &gt; NOW())
               ELSE (notice_display_days IS NULL
                     OR created_at &gt; DATE_SUB(NOW(), INTERVAL notice_display_days DAY))
          END
        )
    </sql>

    <select id="selectActiveForDashboardBoard" resultMap="TwinStudentViolationMap">
        SELECT v.*
        FROM twin_student_violation v
        WHERE <include refid="boardVisibleClause"/>
          AND v.id IN (
              SELECT MAX(id)
              FROM twin_student_violation
              WHERE <include refid="boardVisibleClause"/>
              GROUP BY target_user_id
          )
        ORDER BY v.created_at DESC, v.id DESC
        LIMIT #{limit}
    </select>

    <!-- 管理端标记：同人是否还有其它 ACTIVE、以及哪条正被大屏展示（同人 MAX(id)）。口径与上面严格一致。 -->
    <select id="selectBoardVisibleActiveByUserIds" resultMap="TwinStudentViolationMap">
        SELECT v.*
        FROM twin_student_violation v
        WHERE <include refid="boardVisibleClause"/>
          AND v.target_user_id IN
          <foreach collection="userIds" item="uid" open="(" separator="," close=")">
              #{uid}
          </foreach>
    </select>

    <select id="selectById" resultMap="TwinStudentViolationMap">
        SELECT v.*
        FROM twin_student_violation v
        WHERE v.id = #{id}
    </select>

    <update id="updateEditableById">
        UPDATE twin_student_violation
        SET violation_text = #{violationText},
            content_json = #{contentJson},
            image_urls = #{imageUrls},
            forbid_enter = #{forbidEnter},
            max_enter_success = #{maxEnterSuccess},
            show_notice_every_scan = #{showNoticeEveryScan},
            interactive_challenge = #{interactiveChallenge},
            interactive_challenge_verified_at = #{interactiveChallengeVerifiedAt},
            interactive_unlock_on_verify = #{interactiveUnlockOnVerify},
            expire_at = #{expireAt},
            notice_display_days = #{noticeDisplayDays},
            notice_link_expire = #{noticeLinkExpire},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 单独解除公告：只影响公告可见性，不动 status / forbid_enter / 记录本身。幂等（已解除则 0 行） -->
    <update id="clearNoticeById">
        UPDATE twin_student_violation
        SET notice_cleared_at = NOW(),
            notice_cleared_by_user_id = #{operatorId},
            updated_at = NOW()
        WHERE id = #{id}
          AND notice_cleared_at IS NULL
    </update>

    <!--
      标记「已完成交互处置」。注意：**不能**再要求记录上有 interactive_challenge——
      ACK_READ/QUIZ/SIGNATURE 的答案在待办（twin_obligation）里，记录本身没有短语。
      是否需要交互由 service 的 verifyDispositionAnswer 把关（策略 requiresInteraction），此处不再重复设卡。
    -->
    <update id="acknowledgeInteractiveById">
        UPDATE twin_student_violation
        SET interactive_challenge_verified_at = NOW(),
            forbid_enter = CASE WHEN #{unlockOnVerify} = 1 THEN 0 ELSE forbid_enter END,
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'ACTIVE'
          AND interactive_challenge_verified_at IS NULL
    </update>

    <select id="selectByCageViolationId" resultMap="TwinStudentViolationMap">
        SELECT * FROM twin_student_violation WHERE cage_violation_id = #{cageViolationId} ORDER BY created_at DESC
    </select>

    <update id="setCageViolationId">
        UPDATE twin_student_violation SET cage_violation_id = #{cageViolationId}, updated_at = NOW() WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM twin_student_violation WHERE id = #{id}
    </delete>

    <select id="countActiveAutoStrandedByUserId" resultType="int">
        SELECT COUNT(1)
        FROM twin_student_violation
        WHERE target_user_id = #{targetUserId}
          AND source = 'AUTO_STRANDED'
          AND status = 'ACTIVE'
          AND (
            expire_at IS NULL
            OR expire_at &gt; NOW()
            OR (<include refid="pendingInteractivePastExpireClause"/>)
          )
    </select>

    <select id="tryAcquireLock" resultType="java.lang.Integer">
        SELECT GET_LOCK(#{lockName}, #{timeoutSeconds})
    </select>

    <select id="releaseLock" resultType="java.lang.Integer">
        SELECT RELEASE_LOCK(#{lockName})
    </select>
</mapper>
