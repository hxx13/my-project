<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.notification.push.digest.NotifyDigestItemMapper">
  <resultMap id="BaseResultMap" type="com.example.demo.modules.notification.push.digest.NotifyDigestItem">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="source_code" property="sourceCode"/>
    <result column="channel_code" property="channelCode"/>
    <result column="title" property="title"/>
    <result column="content" property="content"/>
    <result column="status" property="status"/>
    <result column="create_time" property="createTime"/>
    <result column="send_time" property="sendTime"/>
  </resultMap>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO notify_digest_item (user_id, source_code, channel_code, title, content, status)
    VALUES (#{userId}, #{sourceCode}, #{channelCode}, #{title}, #{content}, 'PENDING')
  </insert>
  <insert id="batchInsert">
    INSERT INTO notify_digest_item (user_id, source_code, channel_code, title, content, status) VALUES
    <foreach collection="items" item="it" separator=",">
      (#{it.userId}, #{it.sourceCode}, #{it.channelCode}, #{it.title}, #{it.content}, 'PENDING')
    </foreach>
  </insert>
  <select id="findPendingByUser" resultMap="BaseResultMap">
    SELECT * FROM notify_digest_item WHERE user_id = #{userId} AND status = 'PENDING' ORDER BY create_time ASC, id ASC
  </select>
  <select id="findDistinctPendingUsers" resultType="string">
    SELECT DISTINCT user_id FROM notify_digest_item WHERE status = 'PENDING'
  </select>
  <select id="countPending" resultType="int">
    SELECT COUNT(*) FROM notify_digest_item
    WHERE user_id = #{userId} AND source_code = #{sourceCode} AND status = 'PENDING'
  </select>
  <update id="markSent">
    UPDATE notify_digest_item SET status = 'SENT', send_time = #{sendTime}
    WHERE id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
  </update>
  <delete id="deletePendingBySource">
    DELETE FROM notify_digest_item WHERE source_code = #{sourceCode} AND status = 'PENDING'
  </delete>
  <!-- 定期清理（DigestCleanupTask）：只删已发送且过期的。PENDING 是还没投出去的，绝不能删。
       分片删（LIMIT 一批），走 idx_status_time(status, create_time)。 -->
  <delete id="deleteSentBefore">
    DELETE FROM notify_digest_item
    WHERE status = 'SENT' AND create_time &lt; #{before}
    LIMIT #{limit}
  </delete>
</mapper>
