<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.notification.push.binding.UserPushBindingMapper">
  <resultMap id="BaseResultMap" type="com.example.demo.modules.notification.push.binding.UserPushBinding">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="channel_code" property="channelCode"/>
    <result column="target" property="target"/>
    <result column="is_verified" property="isVerified"/>
    <result column="verify_code" property="verifyCode"/>
    <result column="create_time" property="createTime"/>
    <result column="update_time" property="updateTime"/>
  </resultMap>
  <select id="findByUserAndChannel" resultMap="BaseResultMap">SELECT * FROM user_push_binding WHERE user_id = #{userId} AND channel_code = #{channelCode}</select>
  <select id="findByUser" resultMap="BaseResultMap">SELECT * FROM user_push_binding WHERE user_id = #{userId}</select>
  <select id="findByUserIdsAndChannel" resultMap="BaseResultMap">
    SELECT * FROM user_push_binding WHERE channel_code = #{channelCode} AND is_verified = 1 AND user_id IN
    <foreach collection="userIds" item="uid" open="(" separator="," close=")">#{uid}</foreach>
  </select>
  <insert id="upsert">
    INSERT INTO user_push_binding (user_id, channel_code, target, is_verified, verify_code)
    VALUES (#{userId}, #{channelCode}, #{target}, #{isVerified}, #{verifyCode})
    ON DUPLICATE KEY UPDATE target=VALUES(target), is_verified=VALUES(is_verified),
    verify_code=VALUES(verify_code), update_time=CURRENT_TIMESTAMP
  </insert>
  <update id="updateVerified">UPDATE user_push_binding SET is_verified=#{isVerified}, update_time=CURRENT_TIMESTAMP WHERE user_id=#{userId} AND channel_code=#{channelCode}</update>
  <delete id="deleteByUserAndChannel">DELETE FROM user_push_binding WHERE user_id = #{userId} AND channel_code = #{channelCode}</delete>
</mapper>
