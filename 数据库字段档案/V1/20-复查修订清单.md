# 20-复查修订清单（16/17/18/19 四份深设计的漏洞）

> 状态：**复查结论**。由 4 个子 agent 并行复查 16/17/18/19，对照 Word/Excel 源文档 + 05/06/09/14 既有设计，过滤误报后得到本清单。**按病根归类，附修订动作。**

## 〇、一句话病根

四份深设计是「赶着写」的，**没严格回查三样东西**：03 的真实字段名、05/06 已确认的决策、Word 里的分层/维度细节。导致三类问题：① 编造字段名（19）② 压平真实维度（16/17）③ 跨文档漂移（16/18 与 06 打架）。

## 一、必改（高价值真漏洞）

### A. 19 CDISC —— 最严重，需重写 §四/§五

| # | 漏洞 | 修订 |
|---|---|---|
| A1 | §五 映射示例约 6 成字段名与 03 对不上（body_weight→实为 weight、heart_rate→hr、drug→drug_code、organ_type→tx_organ、rejection_grade→rej_grade） | **逐字段对照 03 用真实 name_en 重写示例** |
| A2 | SDTM 标错，NHP 临床前应对齐 **SEND**（体重 BW 域非 VS、排斥 CL 非 AE） | §四 域映射按 SEND 重排 |
| A3 | 漏 USUBJID 稳定标识传播（D3-D9 记录挂 tx_id 不带 recip_id，导出需两跳 join 回填） | 补「USUBJID 传播与主体建模」一节（受体猴=USUBJID、供体猪独立主体+RELREC、经 tx_id 枢纽回填） |

### B. 16 数据导入 —— 落库链路两个硬断点

| # | 漏洞 | 修订 |
|---|---|---|
| B1 | 导入缺 subject/visit/record 键映射，第 5 步「落库写 record_value」无从写起 | mapping_json 增加保留键列（__subject_code__/__tx_id__/__visit_tp__），定义「找不到即建/报错」 |
| B2 | 审计留痕引用不存在的列：source_type/source_ref 是 crf_record_value 的列，不是 crf_data_audit_log 的 | 统一由 06 定：审计层只记 change_reason/operator/before/after，来源列落在 record_value |
| B3 | mapping_json 扁平结构扛不住重复组/多 sheet/宽表（D6 每次给药、D10 每3h） | 映射升级为分层结构：sheet→form、列组→字段/重复组实例、聚合/拆分规则 |

### C. 17 逻辑引擎 —— 三处压平了真实维度

| # | 漏洞 | 修订 |
|---|---|---|
| C1 | RANGE 扁平 {min,max} 表达不了「血肌酐按种属/性别/年龄分层参考范围」 | 增 crf_reference_range 表（field+种属+性别+年龄带+min/max+版本），RANGE 引用该表 |
| C2 | 访视窗口 ±window_days 对称单值表达不了 TP01(-28~-7d)/TP12(+30d)/TP10(无锚点) | crf_visit 增 early_days/late_days 两列，事件触发类时点跳过窗口校验 |
| C3 | TIME_GAP 引用 visit_date（访视实例属性）却挂在字段级 field_id 上 | 明确为跨实体规则，引用 visit_instance 而非 field_id |

### D. 18 后端 —— 没消化 06 已定决策

| # | 漏洞 | 修订 |
|---|---|---|
| D1 | 冻结粒度矛盾：字段级 /freeze + 表单级 publish 并存，06 已定「冻结=表单级」 | 删字段级 freeze/retire，冻结收敛到表单级 publish |
| D2 | 双录入+签名端点缺失（06 已确认一期），crf_signature 成孤儿 | 补 double-entry/compare/sign 端点 |
| D3 | 8 张表无端点（study/center/visit/validation_rule/dag/dag_user/form_role/id_rule） | 补 CRUD，或显式标注「种子/配置表不走 REST」 |
| D4 | ID 编码 16 类只落 8 类，SMP 写成 SAMPLE，前后漂移 | 补全 16 类，统一 SMP |

## 二、跨文档对齐项（四份 + 05/06 必须统一）

| 统一项 | 现状冲突 | 定案 |
|---|---|---|
| 冻结粒度 | 05 字段级 vs 06 表单级 | **表单级**（06 定） |
| 审计来源列 | source_type/source_ref 被误写到 audit_log | **落在 crf_record_value** |
| source_type 命名 | value 层 MANUAL/IMPORT/PAPER vs batch 层 CSV/EXCEL/PAPER 同名异义 | value 层 `entry_mode`、batch 层 `file_format` |
| change_reason 枚举 | 06「录入/修正/query回复/导入」 vs 17「校验触发」未注册 | 补「校验触发」进 06 枚举 |
| ID 编码 | SMP/SAMPLE 漂移、16 类只落 8 类 | 统一 16 类 + SMP |

## 三、次要（中价值，二期补）

- 17 丢 SKIP/条件必填两类规则（05 有）——补 CONDITIONAL_REQUIRED/BRANCH 类型
- 17 缺「标记」第三态（WARN/ERROR 压不下「血药浓度低→标记」）——severity 加 FLAG，接 query 自动开单
- 16 缺 collected_by（采集人，ALCOA+ Attributable）——增列
- 16 双轨一致性核查无数据源（纸版原始值不落库）——Excel 兜底走 IMPORT 留原始值
- 16 采集时间无时区/缺失兜底——统一 UTC+8 + 回退规则
- 19 供体猪域 D1 归 SUPPQUAL 不自洽（是一级实体非限定词）——用自定义域 + RELREC
- 19 复合文本字段无原子值（creatinine 藏在 renal_baseline）——先原子化才能映射 LB
- 18 码表版本化被绕过（items 直接 PUT/DELETE）——补 freeze + 仅 DRAFT 可改

## 四、修订动作建议

1. **先回改 19**（最严重，字段名编造是硬伤）——逐字段对照 03 重写映射示例。
2. **再回改 16/17/18**——按上面 A/B/C/D 的表逐条改。
3. **最后做一轮 05/06 的横向对齐**——把「冻结粒度、审计列、source_type 命名、change_reason、ID 编码」五个统一项固化进 05/06，作为唯一权威。

## 五、修复状态（2026-08-20 已执行）

| 项 | 状态 |
|---|---|
| 5 个跨文档对齐项（冻结/审计列/source_type/change_reason/ID） | ✅ 已固化（06/16/18 已改，复检无残留） |
| 19 字段名编造 + SDTM→SEND + USUBJID 传播 | ✅ 已重写（§四/§五/§七） |
| 16 键映射 + mapping 分层 | ✅ 已补（保留键列 + repeat 结构） |
| 17 分层参考范围 + 非对称窗口 + 跨实体 + 第三态 | ✅ 已补（crf_reference_range 表 + early/late_days + FLAG + CONDITIONAL_REQUIRED） |
| 18 冻结对齐 + 双录入签名端点 + 孤儿表 + ID 16 类 | ✅ 已改 |

**二期（中低价值，暂缓）**：16 的 collected_by（采集人）、双轨一致性数据源、时区；17 的 ERROR→自动开 query 链路；19 的复合字段原子化。
