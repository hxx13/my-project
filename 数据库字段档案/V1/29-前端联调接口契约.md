# 前端联调接口契约与待办清单

> 面向后端同事。前端已按 00–27 设计档案完成页面改造（配置侧 / 治理侧 / 采集侧 / 总览），
> 全部 API client 已按契约定义在 `frontend/src/features/nhp/api/` 下，`tsc -b --noEmit` 通过。
> 本文档列出后端需落地的 DDL 与接口；**2026-08-22 联调执行后 P0/P1 端点已实现**（见 §三状态列）。

---

## 一、数据库 DDL 依赖（需加的列 / 表）

来源引用：22 §2/§3/§6。

| 表 | 需加列 / 改动 | 出处 | 状态 |
|---|---|---|---|
| `crf_form` | `capture_form` + `event_anchor` + `frequency` | 22 §6.1 | ✅ V34/V35 |
| `crf_field` | `verdict` + `verdict_note` + `concept_code` | 22 §6.5① / §2.2 | ✅ V26/V36 |
| `crf_codelist_item` | `verdict` + `verdict_note` | 22 §6.5① | ✅ V36 |
| `crf_visit` | `event_anchor` + `end_days` | 22 §2.1 / §6.2 | ✅ V25/V34 |
| `crf_subject` | `lifecycle_stage`；`arm_code` | 22 §6.5③ | ✅ V38 |
| `crf_sequence` | `(id_type, scope_key)` + `scope_key` | 22 §4.1 | ✅ V27 |
| `crf_id_rule` | `derived` | 22 §4.2 | ✅ V27 |
| `crf_record` | `atom_id` + `transplant_id` | 22 §6.2 | ✅ V34 |
| `crf_visit_instance` | `transplant_id` | 22 §6.2 | ✅ V34 |

新建表：

| 表 | 状态 |
|---|---|
| `crf_timepoint_map` / `crf_event_rule` / `crf_todo` / `crf_quality_event` / `crf_visit_plan` / `crf_standard_version` | ✅ V25–38 |
| `crf_record_snapshot` / `crf_data_audit_log` / `crf_dict_change_log` | ✅ 更早迁移已有 |
| **`crf_notification`** | ✅ **V39**（2026-08-22） |

---

## 二、枚举定义（前端已定义，后端需对齐取值）

| 枚举 | 取值 |
|---|---|
| `capture_form` | `SERIES` / `LEDGER` / `PANEL` |
| `event_anchor`（16） | `ENROLL` / `PRE_TX` / `DAY0` / `POST_TX` / `INTRAOP` / `ANES` / `PERFUSION` / `HARVEST` / `SAMPLE` / `READOUT` / `REGIMEN` / `STORAGE` / `EVENT` / `ENDPOINT` / `LOCK` / `ALL` |
| `frequency`（15） | `ONCE` / `PER_TP` / `PER_DOSE` / `Q3H` / `Q1_3H` / `Q15_30MIN` / `HOURLY` / `BIWEEKLY` / `QUARTERLY` / `ANNUAL` / `PER_LAB` / `PER_PROTOCOL` / `CONTINUOUS` / `EVENT` / `PER_EVENT` |
| `verdict`（4） | `CONFIRM` / `MODIFY` / `DELETE` / `QUESTION` |
| `lifecycle_stage`（4） | `SCREENING` / `MATCHING` / `POST_TX` / `ENDPOINT` |
| `quality_event_type`（4） | `OUTLIER` / `DEVIATION` / `TAT_OVERDUE` / `COC_BROKEN` |
| `quality_event_status`（3） | `OPEN` / `REVIEWED` / `CLOSED` |
| `todo.status`（3） | `OPEN` / `DONE` / `CANCELLED`（`OVERDUE` 查询派生） |
| `notification.type`（4） | `REVIEW` / `QUALITY` / `TODO` / `VERSION` |

---

## 三、后端接口契约（按前端 API 文件）

统一前缀 `/api/nhp`，响应 `Result<T> = { code, success, message, data }`。前端 `authHttp` 解包。

**状态说明**：✅ 已实现 · ♻️ 别名/并存 · ⚠️ 占位 KPI（结构对齐，检测器 stub）

### 配置侧

| 前端文件 | 端点 | 方法 | 状态 | 实现定案 |
|---|---|---|---|---|
| `nhpVisit.api.ts` | `/nhp/visits` | GET / PUT `/{id}` | ✅ | `NhpVisitController`；实体 camelCase |
| | `/nhp/timepoint-map` | GET | ✅ | 同上 |
| `nhpIdRule.api.ts` | `/nhp/idrules` | GET / PUT `/{id}` | ✅ | `NhpIdRuleController`；取号仍走 `POST /nhp/ids/next` |
| `nhpEventRule.api.ts` | `/nhp/event-rules` | GET / PUT `/{id}` | ✅ | `NhpEventRuleController` |
| `nhpStandard.api.ts` | `/nhp/standards` | GET | ✅ | `NhpStandardController` |
| `nhpTemplate.api.ts` | `/nhp/templates` 系列 | 加字段 | ✅ | 响应/保存含 `captureForm`/`eventAnchor`/`frequency` |

### 治理侧

| 前端文件 | 端点 | 方法 | 状态 | 实现定案 |
|---|---|---|---|---|
| `nhpQuality.api.ts` | `/nhp/quality/events` | GET | ✅ | `NhpQualityController` |
| | `/nhp/quality/monthly-report` | GET | ⚠️ | KPI 键名对齐前端：`doubleEntryRate`/`outlierClosedRate`/`tatOnTimeRate`/`deviationRate`/`cocOpenCount`（检测器仍 stub，值可为 null） |
| `nhpAudit.api.ts` | `/nhp/data-audit-log` | GET | ✅ | `NhpGovernanceController`；响应含 `fieldCode`（JOIN）+ `operator` |
| | `/nhp/dict-change-log` | GET | ✅ | 同上 |
| `nhpSnapshot.api.ts` | `/nhp/snapshots` | GET | ✅ | 全局列表；`version`←`version_no`；与 per-record 快照并存 |
| | `/nhp/snapshots/{id}/diff` | GET | ✅ | query `otherId` |
| `nhpCodelistReview.api.ts` | `/nhp/codelists/{code}/review-items` | GET | ✅ | 与 approve/publish/unfreeze **并存** |
| | `.../items/{id}/verdict` | POST | ✅ | |
| | `.../freeze` | POST | ♻️ | alias：PENDING→approve；DRAFT→publish |

### 采集侧

| 前端文件 | 端点 | 方法 | 状态 | 实现定案 |
|---|---|---|---|---|
| `nhpSubjectBoard.api.ts` | `/nhp/subjects/board` | GET | ✅ | `NhpGovernanceController`；可选 `armCode` |
| `nhpWorkbench.api.ts` | `/nhp/query/listTodoBySubject` | GET | ✅ | 逾期派生 `OVERDUE` |
| | `/nhp/query/listSeries` | GET | ✅ | **主路径**；别名 `/query/series`。形状一律 `{indicators,rows}`（无 conceptCode=多指标网格） |

### 总览

| 前端文件 | 端点 | 方法 | 状态 | 实现定案 |
|---|---|---|---|---|
| `nhpOverview.api.ts` | `/nhp/overview` | GET | ✅ | KPI+审核进度+通知+待办+病例+版本+动态 |
| `nhpTask.api.ts` | `/nhp/query/listMyTasks` | GET | ✅ | FIELD_REVIEW/RECORD_REVIEW/SIGN/QUESTION |
| | `/nhp/notifications` | GET | ✅ | 依赖 **V39** `crf_notification`；响应 `group`←`type` |

### 既有保留（不删）

`/templates` `/fields` `/codelists` `/subjects` `/records` `/ids/next` `/seed` `/imports/*` `/query/concepts` `/records/{id}/audit|snapshots|double-entry|sign|queries` 等全部继续可用。

---

## 四、三形态 dispatch 依赖

- 前端 `NhpFillWorkbench` 读 `templateMeta.captureForm`；后端模板 JSON 已暴露该字段。
- `listSeries` 已返回 `{indicators,rows}`，可直接喂 `NhpSeriesGrid`。

---

## 五、待定项（仍开放）

1. **`arm_code` 业务规则**：列已落库（V38），臂切换器 UI 可按 `armCode` 过滤；独立 `crf_arm` 表仍不建。
2. **质量检测器实算**：月报 KPI 键名已对齐，OUTLIER/DEVIATION/TAT/CoC 检测器仍为 stub。
3. **审核中心按角色/DAG 细过滤**：`listMyTasks` 当前聚合全站队列，未按登录人 DAG 裁剪（28 §七后置）。
4. **批量连录 `upsertSeries`**：接口级后置。

---

## 六、前端文件对照表（改造后）

| 层 | 前端文件 | 对应后端 Controller/表 |
|---|---|---|
| 配置 | `NhpTemplateEditor` | NhpTemplateController + crf_form |
| 配置 | `NhpVisitPage` | NhpVisitController |
| 配置 | `NhpIdRulePage` | NhpIdRuleController + POST /ids/next |
| 配置 | `NhpEventRulePage` | NhpEventRuleController |
| 配置 | `NhpStandardPage` | NhpStandardController |
| 治理 | `NhpQualityPage` | NhpQualityController |
| 治理 | `NhpAuditPage` | NhpGovernanceController（全局审计） |
| 治理 | `NhpSnapshotPage` | NhpGovernanceController（全局快照） |
| 治理 | `NhpCodelistReviewPage` | NhpCodelistController review/verdict/freeze |
| 采集 | `NhpSubjectBoardPage` | /nhp/subjects/board |
| 采集 | `NhpTimelineStrip`/`NhpSeriesGrid`/`NhpLedger` | listSeries / listTodoBySubject |
| 总览 | `NhpOverviewPage` | /nhp/overview |
| 总览 | `NhpReviewCenterPage` | listMyTasks |
| 总览 | `NhpNotificationCenterPage` | /nhp/notifications（V39） |
| 运维 | `nhpOps.api.ts` + 流程前门 | /seed · /ids/next · /query/concepts · /imports |
