# 启动文案（新对话粘贴用）

> 复制下面整段到新对话即可。

---

接着做「NHP 异种移植 CRF/EDC」这套系统。前面的架构设计和代码骨架已经做得差不多了，成果都堆在 `d:\codex\verson.1.2\20260416\数据库字段档案\` 这个目录里——二十来份文档，从数据字典、表结构、业务流转，一路到前后端设计，另外还附了一份「检索指南」和一份「复查结论」。

你先自己去翻一遍，从 `00-检索指南.md` 和 `README.md` 看起。`20-复查修订清单.md` 里记着上一轮踩过的坑，重点看——尤其记住两条：字段名一定对照 Excel 里的真实字段，别自己编；各文档之间有统一约定，别写岔了。

翻完你就明白现在卡在哪了：前端骨架已经搭好、类型检查也过了，后端的 Java 和建表 SQL 还没动，而文档里已经把接下来每一步怎么走都写清楚了。顺着往下推就行，该写代码直接写；碰到「写库」「注册启动链」这种不可逆的动作，先停下来问我一句。

---

（设计背景速览，供你心里有底：核心是一条「移植事件 TX」贯穿供体→受体→配型→手术→样本→随访→结局的链路；数据库 25 张表，分元数据/数据/导入三层；前端字段字典和表单模板两层分离，题型在表单模板层自由配置。细节都在文档里。）
